/**
 * @ngdoc service
 * @name BP.<%=_.camelize(moduleName)%>.<%=_.camelize(name)%>
 * @description
 */

class <%=_.classify(name)%>Model {
  constructor(api) {
    'ngInject';
  }
}

export default <%=_.classify(name)%>Model;
